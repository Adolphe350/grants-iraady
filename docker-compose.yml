version: "3.8"
services:
  grants:
    build: ./app
    container_name: grants-iraady
    restart: unless-stopped
    ports:
      - "5041:5000"
    volumes:
      - grants_data:/data
    environment:
      - INGEST_SECRET=uwezogrants2026
      - SITE_PASSWORD=Kigali2020@
      - SECRET_KEY=g4r4nts-kigali-session-2026
    labels:
      - "coolify.managed=true"

volumes:
  grants_data:
